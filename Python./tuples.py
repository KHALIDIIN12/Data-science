# tuples are like lists, but they are immutable, meaning that once they are created, their elements cannot be changed. 
# Tuples are defined using parentheses () instead of square brackets [].
numbers = (1, 2, 3, 4, 5)
numbers[0] = 10 # this will raise an error because tuples are immutable and their elements cannot be changed